<?xml version="1.0" encoding="utf-8"?>
<network-security-config>
    <!-- Production: pin certificates and disallow cleartext -->
    <domain-config cleartextTrafficPermitted="false">
        <domain includeSubdomains="true">api.dsrv-developer.my.id</domain>
        <!--
        TODO: Add actual certificate pins for production
        <pin-set expiration="2027-01-01">
            <pin digest="SHA-256">base64-encoded-cert-hash-1</pin>
            <pin digest="SHA-256">base64-encoded-cert-hash-2</pin>
        </pin-set>
        -->
    </domain-config>

    <!-- MinIO / local dev: allow cleartext -->
    <domain-config cleartextTrafficPermitted="true">
        <domain includeSubdomains="true">localhost</domain>
        <domain includeSubdomains="false">192.168.100.50</domain>
    </domain-config>

    <!-- Specific dev server IPs for development -->
    <domain-config cleartextTrafficPermitted="true">
        <domain includeSubdomains="false">192.168.1.104</domain>
        <domain includeSubdomains="false">192.168.100.50</domain>
    </domain-config>
    <!-- TODO: Add certificate pinning before production. Use <pin-set> to pin specific cert hashes. -->
</network-security-config>
